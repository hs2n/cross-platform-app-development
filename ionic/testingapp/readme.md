App *sollte* funktionieren, in `compileAndStartApp.ps1` stehen die commands drinnen die ich benutzt habe
Vielleicht vorher noch `npm install` und `ionic state restore --plugins` oder sowas auf die Art